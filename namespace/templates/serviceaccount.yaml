{{- range $sa := .Values.serviceAccounts }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ $sa.name }}
  {{- if $sa.namespace }}
  namespace: {{ $sa.namespace }}
  {{- end }}
---
{{- end }}
