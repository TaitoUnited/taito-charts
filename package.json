{
  "name": "taito-charts",
  "scripts": {
    "create": "sh -c 'helm create ${0}'",
    "package": "sh -c 'helm package -d ./docs ${0} && npm run index'",
    "index": "helm repo index docs --url https://taitounited.github.io/taito-charts"
  }
}
